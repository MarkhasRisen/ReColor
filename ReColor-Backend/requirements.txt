# ReColor TensorFlow Colorblind Detection System
# Dependencies for GPU-accelerated real-time color detection and CVD simulation

# Core TensorFlow and Machine Learning
tensorflow>=2.13.0
tensorflow-gpu>=2.13.0  # Optional: for explicit GPU support (included in tensorflow>=2.4)

# Computer Vision and Image Processing
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# Scientific Computing and Data Processing
scipy>=1.11.0
scikit-learn>=1.3.0  # For K-means clustering and ML utilities
matplotlib>=3.7.0     # For color visualization and analysis

# Data Processing and Analysis
pandas>=2.0.0  # For advanced CSV processing if needed

# Logging and Data Handling
colorsys  # Built-in Python module for color space conversions

# System and Performance
psutil>=5.9.0  # For system monitoring

# Development and Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0

# GPU Acceleration Notes:
# - TensorFlow 2.13+ includes CUDA and cuDNN support
# - Ensure NVIDIA GPU drivers are up to date
# - CUDA 11.8+ and cuDNN 8.6+ recommended for optimal performance
#
# For CPU-only installation, use:
# tensorflow-cpu>=2.13.0

# Installation command:
# pip install -r requirements.txt
#
# For GPU support, also ensure:
# - NVIDIA GPU with compute capability 3.5+
# - NVIDIA Driver 450.80.02+
# - CUDA Toolkit 11.8+ (usually installed automatically with TensorFlow)
# - cuDNN 8.6+ (usually installed automatically with TensorFlow)
enum34>=1.1.10       # Enum support for older Python versions (if needed)

# Development and Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0        # Code formatting
flake8>=6.0.0        # Code linting

# Optional: Jupyter for experimentation
jupyter>=1.0.0
matplotlib>=3.7.0    # For plotting and visualization
seaborn>=0.12.0      # Statistical plots

# System Requirements Notes:
# - Python 3.8 or higher
# - For GPU support:
#   * NVIDIA GPU with CUDA capability 3.5+
#   * CUDA Toolkit 11.8 or 12.x
#   * cuDNN 8.x (for TensorFlow)
# - Webcam or camera device for real-time detection
# - Minimum 4GB RAM (8GB+ recommended for training)
# - 2GB+ free disk space for datasets and models

# Installation Instructions:
# 1. Install Python 3.8+
# 2. Create virtual environment: python -m venv recolor_env
# 3. Activate environment:
#    - Windows: recolor_env\Scripts\activate
#    - Linux/Mac: source recolor_env/bin/activate
# 4. Install dependencies: pip install -r requirements.txt
# 5. For GPU support, install appropriate CUDA toolkit
# 6. Verify installation: python main.py --help